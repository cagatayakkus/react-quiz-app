import { startRunning, abortRunning } from "./RunningActions";

import {
  resetQuestionCounter,
  incrementQuestionCounter,
} from "./QuestionCounterActions";

import {
  incrementCorrect,
  incrementWrong,
  incrementPassed,
  resetStatus,
} from "./StatusActions";

export {
  startRunning,
  abortRunning,
  resetQuestionCounter,
  incrementQuestionCounter,
  incrementCorrect,
  incrementWrong,
  incrementPassed,
  resetStatus,
};
